# ===========================================
# Bistro Aurora Backend - Environment Variables
# ===========================================
# Kopiere diese Datei zu .env und fülle die Werte aus

# ===========================================
# PayPal Konfiguration
# ===========================================
# Erhalte deine Credentials von: https://developer.paypal.com/
PAYPAL_CLIENT_ID=your_paypal_client_id_here
PAYPAL_CLIENT_SECRET=your_paypal_secret_here
# Umgebung: 'sandbox' für Tests, 'live' für Production
PAYPAL_ENV=sandbox

# ===========================================
# SMTP E-Mail Konfiguration
# ===========================================
# Für Bestellbestätigungen an Kunden und Restaurant
SMTP_HOST=smtp.example.com
SMTP_PORT=587
# 'true' für SSL/TLS (Port 465), 'false' für STARTTLS (Port 587)
SMTP_SECURE=false
SMTP_USER=your_email@example.com
SMTP_PASS=your_email_password

# Absender-Adresse für ausgehende E-Mails
MAIL_FROM=bistro-aurora@example.com
# Restaurant-E-Mail für Bestellbenachrichtigungen
MAIL_TO_RESTAURANT=restaurant@bistro-aurora.de

# ===========================================
# Server Konfiguration
# ===========================================
# Port auf dem der Server läuft
PORT=5000
# Umgebung: 'development' oder 'production'
NODE_ENV=development
